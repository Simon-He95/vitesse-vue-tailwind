/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./src/**/*.{vue,ts,tsx}'],
  plugins: [
  ],
  theme: {
    extend: {
      // borderRadius: ({ theme }) => ({ ...theme('spacing') }),
    },
  },
}
